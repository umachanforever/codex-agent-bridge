# Security policy

当前维护 `main` 分支。尚无长期支持或安全响应时限承诺。设计边界见 [安全模型](docs/security.md)。

## 报告漏洞

不要公开提交可用凭据、登录链接、生产 transcript 或未脱敏日志。

优先使用 GitHub Security 页面的 **Report a vulnerability**（维护者须先启用私人漏洞报告）。若该入口未启用，只提交不含漏洞细节的联络请求，请维护者建立私下渠道后再提供复现。当前不声明尚未配置的安全邮箱。

报告应包含受影响提交、部署方式、攻击前提、预期与实际行为，以及使用合成数据的最小复现。发现凭据泄漏时先撤销或轮换，不要等待代码修复。

## 支持的安全边界

- 默认只暴露宿主机回环地址；示例不支持直接公网部署。
- 管理口令、客户端 key 与上游登录凭据分离。
- 多个 key 不是多租户隔离，不能互相隔离文件系统或 Codex 会话。
- 原生本地免登录仅用于可信直连环境，不能置于代理或隧道后。
- 完整执行权限可能读取进程可访问的凭据；容器也不应挂载 Docker socket 或整个宿主机目录。
- 日志按敏感明文处理。不要将诊断日志直接上传 issue。
